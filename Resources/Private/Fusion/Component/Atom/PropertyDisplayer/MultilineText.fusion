prototype(Milly.CrudUI:Component.PropertyDisplayer.MultilineText) < prototype(Neos.Fusion:Component) {

    @apply.props = ${props}

    renderer = ${String.nl2br(props.propertyValue ? props.propertyValue : '')}
}

prototype(Milly.CrudUI:Component.PropertyDisplayer.Date) < prototype(Neos.Fusion:Component) {

    @apply.props = ${props}
    renderer =${props.propertyValue ? Date.format(props.propertyValue, "d.m.Y") : ''}

}

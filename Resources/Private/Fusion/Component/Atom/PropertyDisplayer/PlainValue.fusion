prototype(Milly.CrudUI:Component.PropertyDisplayer.PlainValue) < prototype(Neos.Fusion:Component) {

    @apply.props = ${props}

    renderer = ${props.propertyValue != null ? props.propertyValue : ''}
}
